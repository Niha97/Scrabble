body {
  font-family: Roboto, sans-serif;
  margin: auto;
  display: flex;
  flex-direction: column;
}

.main {
  max-width: 70rem;
  margin: auto;
  min-height: 80vh;
  text-align: center;
  vertical-align: middle;
}
